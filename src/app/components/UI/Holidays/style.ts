import { styled } from '@mui/system';
import { Box } from '@mui/material';

export const HolidaysList = styled(Box)({
  fontSize: '14px',
});

export const HolidaysListItem = styled(Box)({
  background: 'blue',
  borderRadius: '4px',
  wordWrap: 'break-word',
  color: 'white',
  marginTop: '4px',
  marginBottom: '4px',
  padding: '4px',
});
